#include "lipch.h"
#include "StagingBuffer.h"

namespace Li
{
    Ref<StagingBuffer> StagingBuffer::Create(uint32_t size)
    {
        // TODO
        return Ref<StagingBuffer>();
    }
}
